

task :run do |t, args|
    puts "Cleaning packages"

    # manager = CsvManager.new('data/db.csv')

    # manager.run

    puts 'done'
end

task :build do |t, args|
    puts "Current env is #{ENV['RAKE_ENV']}"
  end

  task :add do
    puts ENV['NUM1'].to_i + ENV['NUM2'].to_i
  end